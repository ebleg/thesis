%% if level == 1
\section*{\VAR{title}}
%% elif level == 2
\subsection*{\VAR{title}}
%% else
\subsubsection*{\VAR{title}}
%% endif
% Greek symbols
\begin{itemize}[itemsep=0pt, leftmargin=2cm, labelsep=0cm, labelwidth=1.9cm, align=left]
%% for symbol in symbols
    %% if "greek" in symbol.tags
    \item[$\VAR{symbol.latex_string}$] \VAR{symbol.explanation}
    %% endif
%% endfor
%
% --------- Letter symbols
%% for symbol in symbols
    %% if "letter" in symbol.tags
    \item[$\VAR{symbol.latex_string}$] \VAR{symbol.explanation}
    %% endif
%% endfor
%
% ---------- Number sets
%% for symbol in symbols
    %% if "numberset" in symbol.tags
    \item[$\VAR{symbol.latex_string}$] \VAR{symbol.explanation}
    %% endif
%% endfor
%
% ---------- Groups
%% for symbol in symbols
    %% if "group" in symbol.tags
    \item[$\VAR{symbol.latex_string}$] \VAR{symbol.explanation}
    %% endif
%% endfor
%
% ---------- Other symbols
%% for symbol in symbols
    %% if "misc" in symbol.tags
    \item[$\VAR{symbol.latex_string}$]  \VAR{symbol.explanation}
    %% endif
%% endfor
\end{itemize}
